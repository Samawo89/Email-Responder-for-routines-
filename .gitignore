# Dependencies
node_modules/
package-lock.json
yarn.lock

# Environment variables
.env
.env.local
.env.*.local

# Logs
# NOTE: the routine's run logs (logs/*.md) and learned-styles/ ARE committed on purpose,
# so the "learn over time" memory persists across runs (incl. remote scheduled runs).
*.log
legacy/processing.log.json

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Credentials (never commit these!)
credentials.json
service-account.json
*.pem
*.key
